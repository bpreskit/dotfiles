---
all:
  children:
    home:
      children:
        home_servers:
        home_clients:
    home_servers:
      hosts:
        bristream:
    home_clients:
      hosts:
        owlfrog:
        math-blaster:
