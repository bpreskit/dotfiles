all:
  children:
    home:
      children:
        home_servers:
          hosts:
            bristream:
              host: localhost
              ansible_connection: local
        home_clients:
          hosts:
            owlfrog:
            math-blaster:
    work:
      children:
        work_vms:
          hosts:
            vm:
